const signup = {
  invalidUsernamePassword: "Invalid username or password",
  invalidUsername: "Usuário invalido"
};

const login = {
  noUserFound: "No user found",
  noPasswordMatched: "No password matched"
};

const auth = {
  failed: "Failed to authenticate"
};

module.exports = {
  signup,
  login,
  auth
};
